# ─── Core PDF & Text Processing ──────────────────────────────────────────────────
pymupdf==1.28.2           # imports as `fitz` — renamed package with pre-built wheels for Streamlit Cloud
tiktoken>=0.8.0            # pre-built wheels available for Streamlit Cloud
langchain-text-splitters==0.0.1

# ─── Data Validation ─────────────────────────────────────────────────────────────
pydantic>=2.6.3
pydantic-core>=2.18.2

# ─── Vector Database ─────────────────────────────────────────────────────────────
supabase>=2.3.0

# ─── Embeddings ──────────────────────────────────────────────────────────────────
# Primary: Jina AI API (jina-embeddings-v5, 1024-dim, free, 100+ languages)
# Fallback: sentence-transformers local models
requests>=2.31.0
numpy>=1.24.0

# ─── Language Detection (free, offline) ──────────────────────────────────────────
langdetect==1.0.9

# ─── LLM — Groq API (Scope Check + Critic + Generator) ──────────────────────────
groq>=0.9.0
# Scope/Critic: allam-2-7b (fast, clean JSON)
# Generator: openai/gpt-oss-20b (clean output, bilingual)

# ─── LLM — AgentRouter (Premium Generator) ──────────────────────────────────────
# Primary generator: GPT-5.6-sol via AgentRouter

# ─── LLM — Gemini (fallback) ────────────────────────────────────────────────────
google-generativeai>=0.8.0

# ─── UI ───────────────────────────────────────────────────────────────────────────
streamlit>=1.32.0

# ─── Utilities ───────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.2.3            # Retry logic for API calls
pandas>=2.0.0              # Eval tables and data display

# ─── HTTP Clients (for compatibility) ──────────────────────────────────────────
httpx>=0.25.0
h11>=0.16.0
